---
name: implement-task
description: Implements one READY Task Packet in an isolated worktree with test-first evidence.
---

# implement-task

Confirm READY status and clean worktree. Follow tests first, keep scope fixed, run exact gates, and write a factual Run Report. Stop instead of expanding scope.

## Hard stops

Secrets/production access, scope expansion, default-branch push, destructive Git, merge, deploy, or release.
